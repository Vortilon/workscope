# Core — pin starlette to avoid API-breaking upgrades
fastapi>=0.115,<1.0
starlette>=0.41,<1.0
uvicorn[standard]
python-dotenv
python-multipart
itsdangerous
bcrypt

# DB
sqlalchemy>=2.0
alembic
aiosqlite

# Excel & files
openpyxl
xlrd>=2.0
pypdf>=3.0
pdfplumber
camelot-py[cv]
Pillow
# Tier 2 — Tesseract OCR (requires tesseract-ocr system package, installed in Dockerfile)
pdf2image
pytesseract
opencv-python-headless
# Tier 3 — Google Document AI (credentials required; safe to leave installed even if unused)
google-cloud-documentai

# HTTP / AI
httpx

# Fuzzy matching
rapidfuzz

# Templates & frontend
jinja2

# Optional: for structured extraction / future AI training hooks
# pydantic-settings

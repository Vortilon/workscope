# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal repository containing a single plaintext file (`helloworld`). There are no build systems, dependencies, services, or automated tests.

- **Lint**: No linter configured.
- **Test**: No test framework configured.
- **Build**: No build step required.
- **Run**: `cat helloworld` prints the file content.

No services need to be started. The update script is a no-op (`echo "No dependencies to install"`).
